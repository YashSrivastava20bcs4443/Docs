            # Check if sign-in page appears
            if "signin" in page.url:
                await login_azure(page)  # Handle sign-in
                
                # Wait for navigation after sign-in
                await page.waitForNavigation()
