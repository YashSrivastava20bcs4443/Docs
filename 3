SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'completed_contacts';


SELECT contact_id, contact_start_date, end_reason, from_addr, to_addr
FROM completed_contacts;
