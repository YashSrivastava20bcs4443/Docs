from selenium.webdriver.common.keys import Keys

html_element = driver.find_element(By.TAG_NAME, "html")

# Send keys to adjust zoom level (Ctrl + - to decrease zoom)
for _ in range(5):  # Decrease zoom level by 5 times (each - key press decreases by 25%)
    html_element.send_keys(Keys.CONTROL, "-")
    time.sleep(1)
