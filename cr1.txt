from selenium.webdriver.common.by import By
import time

# Find all rows containing alerts
rows = driver.find_elements(By.XPATH, "//div[contains(@class, 'fxc-gc-row')]")

# Iterate through each row containing an alert
for index, row in enumerate(rows, 1):
    try:
        # Find the elements within the row containing alert details
        alert_details = row.find_elements(By.XPATH, ".//div[@class='fxc-gc-cell']")
        
        # Extract and print each alert detail
        print(f"Alert {index}:")
        for detail in alert_details:
            print(detail.text)
        
        print()  # Add a newline between alerts for better readability
    
    except Exception as e:
        print(f"An error occurred: {str(e)}")
        continue
