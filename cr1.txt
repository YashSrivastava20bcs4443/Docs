from selenium.webdriver.common.by import By
import time

# Find all alert elements by class name
alert_elements = driver.find_elements(By.CLASS_NAME, "fxc-gcflink-link")

# Iterate through each alert
for index, alert_element in enumerate(alert_elements, 1):
    # Click on the alert element to view details
    alert_element.click()

    # Wait for alert details to load
    time.sleep(5)

    # Find the link within the alert element based on href attribute
    link_to_next_dashboard = alert_element.get_attribute('href')
    
    # Open the next dashboard if the link is not empty
    if link_to_next_dashboard:
        driver.get(link_to_next_dashboard)
        
        # Wait for the next dashboard to load
        time.sleep(5)
        
        # Capture screenshot of the next dashboard
        driver.save_screenshot(f'next_dashboard_{index}.png')

        # Go back to the previous page (alert details)
        driver.back()

        # Wait for page to load
        time.sleep(5)
    else:
        print(f"No link found for alert {index}")

# Quit the driver
driver.quit()
