from selenium.webdriver.common.by import By
import time

# Find all rows containing alerts
rows = driver.find_elements(By.XPATH, "//div[@class='fxc-gc-row']")

# Iterate through each row containing an alert
for row in rows:
    # Find the second link in the row
    second_link = row.find_element(By.XPATH, ".//a[@class='fxc-geflink-link'][2]")
    
    # Click on the second link
    second_link.click()
    
    # Wait for the page to load
    time.sleep(5)  # You might need to adjust the wait time
    
    # Take a screenshot of the page
    driver.save_screenshot("screenshot.png")
    
    # Go back to the previous page (the alert details)
    driver.back()
    
    # Wait for the page to load
    time.sleep(5)  # You might need to adjust the wait time
