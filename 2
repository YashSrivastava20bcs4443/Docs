import base64

# Example Base64 encoded string
encoded_str = "SGVsbG8gd29ybGQh"  # "Hello world!" in Base64

# Decoding Base64
decoded_bytes = base64.b64decode(encoded_str)
decoded_str = decoded_bytes.decode("utf-8")

print("Decoded string:", decoded_str)
