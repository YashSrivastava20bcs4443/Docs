---
- name: Configure Routers
  hosts: routers
  gather_facts: no
  connection: local
  vars:
    router_hostname: "{{ router_hostname }}"
  tasks:
    - name: Set hostname
      ios_config:
        lines:
          - hostname {{ router_hostname }}
