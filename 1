.chart-container {
    padding: 20px;
    width: 100%;
}

.side-by-side-charts {
    display: flex;
    justify-content: center;
    gap: 20px; /* Space between charts */
    flex-wrap: nowrap; /* Prevent wrapping, keep side by side */
    width: 100%;
    max-width: 1200px; /* Optional: Limit total width on large screens */
    margin: 0 auto;
}

.chart {
    flex: 1; /* Equal width for both charts */
    max-width: 50%; /* Ensure they don’t exceed half the container */
    height: 0; /* Start with zero height */
    padding-bottom: 50%; /* Maintain square aspect ratio (height = width) */
    position: relative; /* For absolute positioning of Plotly content */
    overflow: hidden; /* Prevent overflow */
}

.chart > div {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}
