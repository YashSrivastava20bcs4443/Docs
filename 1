# Function to set zoom using JavaScript
def set_zoom(driver, zoom_level):
    zoom_script = f"document.body.style.zoom='{zoom_level}'"
    driver.execute_script(zoom_script)
    time.sleep(2)  # Allow the zoom to take effect
