filter {
  if [fields][log_type] == "linux_logs" {
    grok {
      match => { "message" => "%{SYSLOGBASE}" }
    }
  }
  else if [fields][log_type] == "windows_logs" {
    json {
      source => "message"
    }
  }
}
